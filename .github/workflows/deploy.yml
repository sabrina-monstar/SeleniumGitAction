name: Deploy
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  Workflow-Dispatch-Test:
  
    runs-on: ubuntu-latest #1
    steps:
      - name: Build API
        run: |
          echo "Build API"
          sleep 10s
